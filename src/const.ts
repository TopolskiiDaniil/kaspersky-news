const MAX_VISIBLE_TAGS = 6;

const enum TextShowAllButton {
	Show = 'Show all',
	Hide = 'Hide'
}

const enum TypeStatus {
	Positive = 'Positive',
	Negative = 'Negative'
}

export {TypeStatus, MAX_VISIBLE_TAGS, TextShowAllButton};